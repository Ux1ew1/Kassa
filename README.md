# Kassa React

Веб-приложение кассы, переписанное на React с использованием Vite.

## Технологии

- **React 18** - UI библиотека
- **React Router** - маршрутизация
- **Vite** - сборщик и dev-сервер
- **ES6 модули** - современный JavaScript

## 📱 Запуск на Android через Termux

Для запуска на телефоне см. подробные инструкции:
- **[TERMUX.md](TERMUX.md)** - полная инструкция по установке и настройке
- **[quick-start-termux.md](quick-start-termux.md)** - быстрый старт

## Установка

1. Перейдите в папку проекта:
```bash
cd Kassa-React
```

2. Установите зависимости:
```bash
npm install
```

## Режимы запуска

### Режим разработки

Запустите два терминала:

**Терминал 1** - Backend сервер (API):
```bash
npm run server
```

**Терминал 2** - Frontend dev сервер (React):
```bash
npm run dev
```

- Backend API будет доступен на `http://localhost:3000`
- Frontend приложение будет доступно на `http://localhost:5173`
- Vite автоматически проксирует `/api` запросы на backend сервер

### Режим продакшена

1. Соберите React приложение:
```bash
npm run build
```

2. Запустите сервер (он будет обслуживать и API, и статические файлы):
```bash
npm start
```

Приложение будет доступно по адресу `http://localhost:3000`

## Структура проекта

```
Kassa-React/
├── public/              # Статические файлы
│   ├── normalize.css
│   └── style.css
├── src/
│   ├── components/     # React компоненты
│   │   ├── AdminMenuList.jsx
│   │   ├── Cart.jsx
│   │   ├── ChecksList.jsx
│   │   ├── ItemModal.jsx
│   │   ├── Menu.jsx
│   │   ├── MenuItem.jsx
│   │   └── SearchBar.jsx
│   ├── hooks/          # Кастомные хуки
│   │   ├── useAdminMenu.js
│   │   ├── useChecks.js
│   │   └── useMenu.js
│   ├── pages/          # Страницы
│   │   ├── Admin.jsx
│   │   └── Kassa.jsx
│   ├── utils/          # Утилиты
│   │   ├── api.js
│   │   └── storage.js
│   ├── App.jsx         # Главный компонент
│   ├── main.jsx        # Точка входа
│   └── index.css       # Глобальные стили
├── index.html
├── package.json
└── vite.config.js
```

## Функциональность

### Касса (`/`)
- Просмотр меню товаров
- Добавление товаров в чек
- Управление несколькими чеками
- Расчет суммы и сдачи
- Поиск товаров
- Сохранение состояния в localStorage

### Админ-панель (`/admin`)
- Просмотр и редактирование меню
- Добавление новых позиций
- Удаление позиций
- Переключение видимости позиций
- Поиск по меню

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## Предпросмотр продакшен сборки

```bash
npm run preview
```

## Быстрый старт

### Windows/Linux/Mac

#### Вариант 1: Разработка (рекомендуется)
1. **Терминал 1** - запустите backend:
   ```bash
   npm run server
   ```

2. **Терминал 2** - запустите frontend:
   ```bash
   npm run dev
   ```

3. Откройте `http://localhost:5173` в браузере

#### Вариант 2: Продакшен (всё в одном сервере)
1. Соберите проект:
   ```bash
   npm run build
   ```

2. Запустите сервер:
   ```bash
   npm start
   ```

3. Откройте `http://localhost:3000` в браузере

### Android (Termux)

1. Установите Termux и Node.js:
   ```bash
   pkg install nodejs git
   ```

2. Скопируйте проект на телефон и перейдите в папку

3. Установите зависимости:
   ```bash
   npm install
   ```

4. Соберите и запустите:
   ```bash
   npm run build
   npm start
   ```

5. Откройте `http://localhost:3000` в браузере телефона

📖 **Подробная инструкция**: [TERMUX.md](TERMUX.md) или [quick-start-termux.md](quick-start-termux.md)

## Структура данных

Данные меню хранятся в файле `data/menu.json`. Сервер автоматически создаст этот файл с пустым массивом, если его нет.

